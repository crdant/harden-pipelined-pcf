---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfnorm/rootfs
    tag: v0.2.7

inputs:
- name: terraform-state-input

outputs:
- name: terraform-state-output

run:
  path: harden-pipelined-pcf/tasks/apply-vm-extensioins/aws/task.sh

params:
  opsman_target:
  opsman_user:
  opsman_password:
